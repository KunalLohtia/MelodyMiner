import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  scrollView: {
    // borderWidth: 2,
    // borderColor: 'blue',
    flexGrow: 3,
    maxHeight: 475,
  },
});

export default styles;
