import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  scrollContainer: {
    flexDirection: 'row',
    marginTop: 10,
    marginBottom: 25,
  },
  image: {
    width: 200,
    height: 200,
    marginHorizontal: 5,
    borderRadius: 5,
  },
});

export default styles;
