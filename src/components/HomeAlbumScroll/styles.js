import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  scrollContainer: {
    flexDirection: 'row',
    marginTop: 10,
    marginBottom: 25,
  },
  image: {
    width: 100,
    height: 100,
    marginHorizontal: 5,
    borderRadius: 5,
  },
});

export default styles;
